/* global Promise, describe, it, __dirname, process*/
const {expect, assert} = require('chai');
const util = require(util);

const EasyErrors = require(`../index.js`);

const t = {};
Object.freeze(t);


describe('EasyError', () => {
  describe('istanza e configurazione', () => {
    it('istanza è un proxy', () => {
      
      
    });
  });
  
});
